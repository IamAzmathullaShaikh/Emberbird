# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
dist/
build/

# Virtual environments
.venv/
venv/
env/
python3-env/

# OS artifacts
Thumbs.db
Desktop.ini
.DS_Store
._*

# IDE / Editor
.vscode/
.idea/
*.swp
*.swo
*~
*.sublime-workspace
*.sublime-project

# Build staging & downloads (per-component .gitignore handles these too)
*.7z
*.zip

# Project-specific
.freebuff/

# Release & validation reports
*-report.json
release-metadata.json
checksums.*

# Node
node_modules/

# Manager CI artifact staging (local copies of release assets)
dist_artifacts/
dist_artifacts_ci/

# Clean-room E2E output (scripts/e2e_clean_room.py)
dist_clean/
dist_winget_submission/

# WSA build outputs (tools/build.sh writes to root output/ post-E2;
# tools/build_local.py stages downloads under upstream/download)
output/
download/

# Local restructure residue (never enters git)
MagiskOnWSAOld/

# Generated report outputs written into docs/ by scripts
# (release_pipeline.py, validate_live_installation.py,
# generate_compatibility_report.py) — build residue, never committed.
docs/COMPATIBILITY_REPORT.md
docs/LIVE_INSTALLATION_REPORT.md
docs/RELEASE_HEALTH_REPORT.md
