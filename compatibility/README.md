# Compatibility Hub Data

This directory contains verified application compatibility records for Windows Subsystem for Android (WSA).

## Directory Structure
- `data/`: Canonical JSON records for verified applications (e.g. `PhonePe.json`, `Paytm.json`).
- `submissions/`: Community-contributed compatibility test reports pending verification.
- `templates/`: Standard schema definition (`template.json`).
